import { makeStyles } from "@mui/styles";

const useStyles = makeStyles({
  container: {
    display: "block",
    width: "50%",
    padding: "auto",
    margin:"auto",
  },
  pokemonInfo: {
    display: "block",
    width: "80%",
    margin: "auto",
    marginTop: "40px",
  }
});

export default useStyles;
